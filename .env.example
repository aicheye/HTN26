# ws = real backend (default), mock = in-browser simulator
VITE_SOURCE=ws
VITE_WS_URL=ws://localhost:8080/ws

# Optional: also POST every command to the Sesame firmware JSON API
# VITE_ROBOT_URL=http://sesame-robot.local
