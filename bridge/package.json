{
  "name": "htn-bridge",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env bridge.mjs",
    "test": "node --test voice.test.mjs && node test.mjs",
    "sim": "node sim.mjs"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
